To obtain the length of a value or column use the LENGTH() function.

SELECT LENGTH(<value or column>) FROM <tables>;

SELECT <column> FROM <table> WHERE LENGTH(<column>) <operator> <value>;

SELECT username AS length FROM cutomers WHERE LENGTH(username) < 7;

return numeral value
